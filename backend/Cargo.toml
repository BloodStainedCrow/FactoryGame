[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
bitvec = { version = "1.0.1", features = ["serde"], default-features = false }
const-assert = "1.0.1"
data = { path = "../data" }
itertools = "0.14.0"
static_assertions = "1.1.0"

[dev-dependencies]
proptest = "1.11.0"
data = { path = "../data", features = ["test"] }

[lints]
workspace = true
